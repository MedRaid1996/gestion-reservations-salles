{{!-- 
  Template principal (layout) de l'application
  Ce fichier contient la structure HTML commune à toutes les pages
  Variables attendues : title (titre de la page)
  Le contenu spécifique de chaque page est inséré dans {{{ body }}}
--}}
<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <title>{{title}}</title>
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    {{!-- Inclusion du partial header (menu de navigation) --}}
    {{> header }}

    {{!-- Contenu principal de la page (variable selon la vue) --}}
    <main>
        {{{ body }}}
    </main>

    {{!-- Inclusion du partial footer --}}
    {{> footer }}

</body>
</html>

